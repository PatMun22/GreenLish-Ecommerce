/* eslint-disable react/prop-types */
import "./articleCard.scss";

const ArticleCard = (props) => {
  return <div className="article-card">My Card No. {props.cardNo}</div>;
};

export default ArticleCard;
